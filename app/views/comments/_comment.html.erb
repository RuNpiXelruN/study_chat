<p></p>
<%= comment.body %>
<p> Posted by <%= link_to comment.user.username, profile_path(comment.user) %>, <%= time_ago_in_words(comment.created_at) + ' ago' %></p>

<% if comment.user == current_user %>
  <%= link_to "Edit", edit_comment_path(comment) %>
  <%= link_to "Delete", comment_path(comment), method: :delete %>
  <p>--------------</p>
<% else %>
  <p>--------------</p>
<% end %>

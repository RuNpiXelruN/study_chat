<%= form_for [user, Chat.new] do |f| %>
  <p><%= f.text_field :title, placeholder: "Enter chat name.." %></p>
  <p><%= f.text_area :description, placeholder: "Enter Chat description.." %></p>
  <%= f.submit %>
<% end %>

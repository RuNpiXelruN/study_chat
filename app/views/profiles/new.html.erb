<h1>Hey <%= @user.username %>!!</h1>
<p>Fill out the rest of your profile!</p>

<%= form_for @user.profile do |f| %>
  <p><%= f.file_field :image %></p>
  <p><%= f.text_area :bio, placeholder: "Enter your bio..." %></p>
  <%= f.submit "Save!" %>
<% end %>
